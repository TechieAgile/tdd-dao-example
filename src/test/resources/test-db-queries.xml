<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">

<properties>
	<entry key="getAllBookData">
		select name, isbin, price, author from book
	</entry>

	<entry key="getAuthorAllBook">
		select name, isbin, price, author from book where
		author = :author
	</entry>

	<entry key="getparticularBook">
		select name, isbin, price, author from book where name
		= :name
	</entry>

	<entry key="insertBookData">
		insert into
		= :name
	</entry>
</properties>